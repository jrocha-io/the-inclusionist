{
  "_comment": "Faz o `npx serve docs/research` mandar os headers COOP/COEP → self.crossOriginIsolated = true, exigido pelo build WASM com pthreads do sherpa (SharedArrayBuffer). Sem isto: 'SharedArrayBuffer transfer requires self.crossOriginIsolated'.",
  "headers": [
    {
      "source": "**",
      "headers": [
        { "key": "Cross-Origin-Opener-Policy", "value": "same-origin" },
        { "key": "Cross-Origin-Embedder-Policy", "value": "require-corp" }
      ]
    }
  ]
}
